services:
  backend:
    build:
      context: .
      dockerfile: app/backend/Dockerfile.gpu
    environment:
      ENABLE_QWEN: "true"
      QWEN_ENABLED_IN_UI: "true"
      NVIDIA_VISIBLE_DEVICES: all
      NVIDIA_DRIVER_CAPABILITIES: compute,utility
    gpus: all
